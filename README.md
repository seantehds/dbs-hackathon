## Start Backend
- `npm install`
- `npm install mongodb bcrypt`
- ensure that the `.env` file has the accurate `ATLAS_URI` and `PORT` number
- `npm run dev`

## Start Frontend
- `npm install`
- `npm run dev`
